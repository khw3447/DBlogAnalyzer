<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.dbloganalyzer</groupId>
  <artifactId>dblog-analyzer</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jsqlparser.version>4.9</jsqlparser.version>
    <junit.version>5.10.2</junit.version>
    <mainClass>com.dbloganalyzer.App</mainClass>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.github.jsqlparser</groupId>
      <artifactId>jsqlparser</artifactId>
      <version>${jsqlparser.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>dblog-analyzer</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>

      <!-- Produces a single runnable jar with dependencies bundled, used as
           input to jpackage for the self-contained Windows exe build. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.3</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>${mainClass}</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Builds a native Windows launcher (target/DBLogAnalyzer.exe) around the
      shaded jar, using Launch4j's cross-platform resource compiler (it ships
      native binaries for Linux/Mac/Windows, so this works from any build
      host, unlike jpackage). The exe is a thin launcher, not a bundled
      runtime: it looks for a "jre" folder next to itself first, falling
      back to a registry-detected system JDK/JRE >= 21. See README.md for
      how to attach a Windows JRE for a fully offline closed-network deploy.
      Run with: mvn -Pwindows-exe package
    -->
    <profile>
      <id>windows-exe</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.akathist.maven.plugins.launch4j</groupId>
            <artifactId>launch4j-maven-plugin</artifactId>
            <version>2.5.1</version>
            <executions>
              <execution>
                <id>l4j-gui</id>
                <phase>package</phase>
                <goals>
                  <goal>launch4j</goal>
                </goals>
                <configuration>
                  <headerType>gui</headerType>
                  <jar>${project.build.directory}/${project.build.finalName}.jar</jar>
                  <outfile>${project.build.directory}/DBLogAnalyzer.exe</outfile>
                  <errTitle>DBLogAnalyzer</errTitle>
                  <classPath>
                    <mainClass>${mainClass}</mainClass>
                  </classPath>
                  <jre>
                    <path>jre</path>
                    <minVersion>21.0.0</minVersion>
                    <requires64Bit>true</requires64Bit>
                  </jre>
                  <versionInfo>
                    <fileVersion>1.0.0.0</fileVersion>
                    <txtFileVersion>1.0.0</txtFileVersion>
                    <fileDescription>DBLogAnalyzer</fileDescription>
                    <copyright>DBLogAnalyzer</copyright>
                    <productVersion>1.0.0.0</productVersion>
                    <txtProductVersion>1.0.0</txtProductVersion>
                    <productName>DBLogAnalyzer</productName>
                    <internalName>DBLogAnalyzer</internalName>
                    <originalFilename>DBLogAnalyzer.exe</originalFilename>
                  </versionInfo>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
